services:
  postgres:
    image: postgres:16-alpine
    container_name: study-chinese-postgres
    profiles:
      - local-db
    environment:
      POSTGRES_DB: study_chinese
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./server/prod.sql:/docker-entrypoint-initdb.d/001-prod.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d study_chinese"]
      interval: 5s
      timeout: 5s
      retries: 10

  server:
    container_name: study-chinese-server
    build:
      context: ./server
    env_file:
      - ./server/.env
    environment:
      NODE_ENV: production
      PORT: 5000
      CLIENT_URL: http://localhost:5173
      API_BASE_URL: http://localhost:5173/api/v1
      OCR_PROVIDER: paddle
      OCR_BASE_URL: http://ocr:8000
      OCR_TIMEOUT_MS: 30000
      TTS_PROVIDER: edge
      TTS_EDGE_VOICE: zh-CN-XiaoxiaoNeural
    depends_on:
      ocr:
        condition: service_healthy
    restart: unless-stopped

  ocr:
    container_name: study-chinese-ocr
    build:
      context: ./ocr-service
    environment:
      OCR_LANG: ch
      OCR_DET_MODEL: PP-OCRv6_small_det
      OCR_REC_MODEL: PP-OCRv6_small_rec
      OCR_DEVICE: cpu
      OCR_ENGINE: paddle_dynamic
      OCR_ENABLE_MKLDNN: "false"
    volumes:
      - paddle_models:/root/.paddlex
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"]
      interval: 30s
      timeout: 5s
      start_period: 60s
      retries: 3
    restart: unless-stopped

  client:
    container_name: study-chinese-client
    build:
      context: ./client
    ports:
      - "5173:80"
    depends_on:
      server:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  paddle_models:
